# CLAUDE.md — RIDE IT (떨어진다!!!) 작업 지침

Claude Code는 이 파일을 세션 시작 시 자동으로 읽습니다. 새 세션이라도 아래 내용으로
이전 대화의 맥락을 복원할 수 있습니다. **작업 시작 전 `개발기록.md`도 함께 읽어주세요.**

## 프로젝트 정체
- 이름: **떨어진다!!!** (영문/리포명: `ride-it`)
- chaechae 스튜디오(hobbyist 브라우저 게임 제작자 Gon)의 신작
- 실제 유명 롤러코스터(과거 명칭: 우방타워랜드/도투락월드/자연농원)를 모티브로 한 5단계 브라우저 게임
- 엔진: Babylon.js (CDN script 태그 로드, 번들러 없음), WebGL2 기준
- 대상 기기: PC + 모바일 (아이폰 SE2가 저사양 기준점)

## 절대 규칙 (변경 금지)
- **고정 타임스텝 물리**: `main.js`의 60Hz 고정 타임스텝 + 렌더링 보간 구조를 유지할 것.
  프레임 종속 물리로 되돌리지 말 것 (판정 타이밍이 기기별로 흔들리는 원인이 됨).
- **조작 흐름**: 스타트(드래그→릴리스, 3인칭 고정) → 주행 중(좌우 밸런스 스와이프 + 부스트/브레이크 게이트 탭 + 손들기 에어타임 홀드) → 피니쉬(탭 타이밍)
- **카메라**: 스타트 구간 3인칭 고정, 런칭 후에만 1인칭/3인칭 토글 가능
- **iOS Safari 대응**: `setPointerCapture()`는 반드시 try/catch, 더블탭 줌은 JS 레벨 차단, 오디오는 첫 제스처에서 unlock

## 파일 구조
```
ride-it/
├── index.html        # 진입점
├── CLAUDE.md          # 이 파일
├── 개발기록.md          # 누적 제작 기록 — 새 결정사항은 여기 추가
├── js/
│   ├── quality.js     # 그래픽 품질 프리셋(Low/Med/High) + 자동감지/적응형 다운그레이드
│   ├── stages.js       # 5단계 제어점/세그먼트/게이트 데이터 (여기가 밸런스 튜닝 지점)
│   ├── track.js          # 커브 생성 (Catmull-Rom), 세그먼트 매핑
│   ├── cart.js             # 물리(에너지보존), 밸런스/게이트 판정
│   ├── camera.js            # 1인칭/3인칭 토글
│   ├── input.js               # Pointer Events 입력 통합
│   ├── ui.js                   # HUD/스테이지선택/결과화면
│   └── main.js                  # 고정 타임스텝 루프
├── assets/
│   ├── models/    # .glb — Cowork가 관리, ASSET_TASKS.md 참조
│   └── textures/
└── blender/        # .blend 원본 — 배포 제외(.gitignore), NAS 백업용
```

## 작업 시 체크리스트
1. `개발기록.md`를 먼저 읽고 지금까지의 결정사항 파악
2. 물리/조작 관련 수정 시 `stages.js`의 세그먼트 구조(`requiredLean`, `leanWindow`, `gate.timingWindow`)를 건드리게 되는데, 값 변경 시 이유를 `개발기록.md`에 한 줄 남길 것
3. 3D 에셋(glb) 추가/변경은 `assets/` 하위에만 — 코드에서 직접 base64 인라인하지 말 것 (기존 chaechae 단일 HTML 원칙과 달리 이 프로젝트는 멀티파일 구조로 진행하기로 결정됨)
4. 커밋 메시지는 한글로, 무엇을 왜 바꿨는지 간단히 — 이것 자체가 제작 기록 역할을 겸함
5. 큰 설계 판단(새 조작 추가, 스테이지 난이도 재조정 등)이 필요하면 코드를 임의로 크게 바꾸지 말고 먼저 질문할 것

## 아직 구현 안 된 부분
- 트랙 지지대/레일/스테이션 실제 glTF 배치는 완료 (`track.js` `loadTrackMeshes`). 남은 건 레일 이음매/지지대 배치 간격의 시각적 튜닝 정도
- 5단계 무중력(에어타임) 구간 12곳 전체 배치 (재점검 후 10개소로 확장, controlPoints/segments 재설계 완료 —
  12곳 목표는 현재 트랙 길이(1680m)·복귀 구간 구조상 한계선이라 아직 미달. 더 늘리려면 트랙 길이 확장 또는
  복귀 구간 재설계 필요 — 개발기록.md 참고)
- 리더보드(Firebase)는 의도적으로 후순위 — 기본 플레이가 안정된 후 진행
