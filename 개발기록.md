# 떨어진다!!! (RIDE IT) 개발기록

## 프로젝트 개요
- chaechae 스튜디오 신작, 실제 유명 롤러코스터(과거 명칭 사용)를 모티브로 한 5단계 브라우저 게임
- 엔진: Babylon.js (WebGL2, 무료, 웹 배포)
- Blender: 트랙 지지대/카트/배경 모델링, glTF/GLB 익스포트
- 대상 기기: PC + 모바일(아이폰 SE2 기준 최적화)

## 확정된 설계 결정
- **조작**: 드래그/후진키 당김 → 릴리스로 스타트 (3인칭 고정) / 주행 중 좌우 밸런스(스와이프) + 부스트·브레이크 게이트(탭) / 종료 지점 피니쉬 타이밍
- **카메라**: 스타트 시 3인칭 고정, 런칭 이후 1인칭/3인칭 토글 가능 (0.25초 보간 전환)
- **물리**: 에너지 보존 기반(v² = v0² + 2gΔh - 마찰), 커브 위 물리엔진 없이 스크립트 이동
- **게임 루프**: 고정 타임스텝(60Hz) + 렌더링 보간(alpha) — 프레임 드랍 시에도 판정 타이밍 유지
- **스테이지 스케일링**: 기본 스테이지당 +10% 참고선 + 실제 코스터 데이터 기반 개별 프로필로 보정
- **그래픽 옵션**: Low/Medium/High 프리셋, 기기 자동 감지 + 런타임 fps 기반 자동 다운그레이드
- **구조**: 3D 에셋 특성상 멀티파일 구조 채택 (기존 chaechae 단일 HTML 원칙에서 예외)
- **저작권**: 실제 파크 과거 명칭 사용(우방타워랜드/도투락월드/자연농원) — 리스크 낮음, 추후 공개 범위 확대 시 재점검

## 5단계 스테이지 (실제 데이터 기반)
1. **우방타워랜드** (이월드 과거명) — 튜토리얼, 완만한 커브, ~45km/h
2. **도투락월드: 급류의 계곡** (파에톤 모티브) — 인버티드 360도 루프, 72km/h, 1148m
3. **도투락월드: 외줄 타기** (스콜&하티 모티브) — 싱글레일, 밸런스 판정창 좁음
4. **도투락월드: 수직 강하** (드라켄 모티브) — 90도 다이브, 최고높이 70m, 104km/h
5. **자연농원** (T익스프레스 모티브, 최종) — 77도 낙하, 4.5G, 12회 무중력 구간, 1.6km

## 파일 구조
```
roller-coaster-game/
├── index.html
├── js/
│   ├── quality.js   # 그래픽 품질 프리셋 + 자동감지/적응형 다운그레이드
│   ├── stages.js    # 5단계 제어점/세그먼트/게이트 데이터
│   ├── track.js     # 커브 생성, 세그먼트 매핑
│   ├── cart.js       # 물리(에너지보존), 밸런스/게이트 판정
│   ├── camera.js     # 1인칭/3인칭 토글, 전환 보간
│   ├── input.js       # Pointer Events 통합 입력
│   ├── ui.js           # 스테이지 선택/HUD/결과 화면
│   └── main.js          # 초기화 + 고정 타임스텝 루프
├── assets/
│   ├── models/  (.glb 예정)
│   └── textures/
└── blender/  (.blend 원본, NAS 백업용, 배포 제외)
```

## 작업 방식 (채팅 / Claude Code / Cowork 역할 분담)
- **기획/의사결정**: 이 채팅에서 진행 (설계, 밸런스, 네이밍 등 판단이 필요한 대화)
- **코드 구현**: Claude Code — `CLAUDE.md`를 세션 시작 시 자동으로 읽어 맥락 복원, 예약 작업으로 `build-check.yml` 기반 야간 빌드/린트 체크 가능
- **에셋 관리**: Cowork — `assets/` 폴더 지정, `assets/ASSET_TASKS.md`에 정의된 반복 작업(텍스처 최적화, glTF 검증, NAS 백업 동기화) 위임
- 추가된 자동화 기반 파일: `CLAUDE.md`, `assets/ASSET_TASKS.md`, `.github/workflows/build-check.yml`, `package.json`, `.gitignore`

## 다음 할 일 (Not yet implemented)
- Blender에서 실제 트랙 커브 제작 → controlPoints 좌표 반영 (현재는 폐곡선으로 재설계된 손튜닝 좌표)
- 카트/레일/지지대/스테이션 glTF 로드·배치는 완료 (아래 스프린트 기록 참조) — 남은 건 텍스처/디테일링 정도
- 12회 무중력(에어타임) 구간 5단계에 전부 배치 (폐곡선 재설계로 8개소 내외까지 확장, 12곳 목표는 아직 미달)
- 리더보드(Firebase)는 후속 단계에서 진행 예정
- 실기기(SE2) 성능 테스트 및 품질 프리셋 튜닝

## 수정 기록
- `cart.js` `_evaluateSegment`의 밸런스 감속 패널티를 `speed *= 0.995` → `speed *= Math.pow(0.995, dt * 60)`로 변경.
  기존 값은 60Hz 고정 타임스텝(dt=1/60) 기준으로 튜닝되어 있었는데 dt를 반영하지 않아, 다른 dt로 호출되면
  초당 감쇠율이 달라지는 문제가 있었음. 지수화로 dt 무관하게 동일 초당 감쇠율 유지 (dt=1/60일 때 기존과 동일).
- `.claude/skills/run-ride-it/` 추가 — 헤드리스 브라우저(Playwright)로 스테이지 선택→씬 로드→`Cart.launch()`까지
  구동해 콘솔 에러/스크린샷을 확인하는 구동 스킬. `/run-ride-it` 또는 "ride-it 실행/스크린샷" 요청 시 자동 로드됨.
- `main.js`: 진행률 확인용 디버그 라인(`_drawDebugTrackLine`) 렌더링을 제거하고, `assets/models/cart_placeholder.glb`를
  실제 로드해 카트로 사용하도록 변경. 매 고정 스텝마다 `track.getPositionAt(cart.t)` / `getTangentAt(cart.t)`로
  위치·방향(`mesh.lookAt`)을 갱신 (`_loadCartMesh` / `_updateCartMesh`). 런칭 전에는 트랙 시작점(t=0)에 고정 배치.
  cdnjs에는 `babylonjs-loaders` 패키지가 없어 glTF 로더 스크립트는 jsdelivr에서 동일 버전(7.25.0)으로 고정해 `index.html`에 추가.
- `main.js`의 `_loadCartMesh` 로드 대상을 `cart_placeholder.glb` → `cart.glb`(Cowork 실제 모델, 텍스처 포함)로 교체.
  `assets/models/cart_placeholder.glb`는 코드 참조가 없어졌지만 파일은 그대로 둠(에셋 정리는 Cowork/ASSET_TASKS.md 담당).
- `track.js`에 `loadTrackMeshes()` 추가 — `rail_standard/single/hybrid.glb`를 스테이지의 `railType`에 따라 골라
  커브의 실측 호 길이를 따라 인스턴싱 타일링(`_instanceAlongCurve`), `pillar_steel/wood.glb`를 8m 간격으로
  레일 높이(`pos.y`)만큼 Y축 스케일해 지면까지 배치, `station_platform.glb`를 t=0에 1회 배치.
  `stages.js`에 `railType: 'standard'|'single'|'hybrid'` 필드 추가 (3단계=외줄타기→single, 5단계=하이브리드 목재+스틸
  모티브→hybrid, 나머지→standard). 지지대는 railType==='hybrid'면 wood, 그 외엔 steel로 자동 선택 (별도 필드 없음).
  스테이지 재로드(다시 도전) 시 이전 인스턴스가 누적되지 않도록 `Track.dispose()` 추가, `main.js`의 `loadStage`가 호출.
  주의: 로드된 glb들의 `__root__`/자식 노드에 위치 오프셋이 baked-in 되어 있었지만 `createInstance()`는 소스 메시의
  자체 position/rotation/parent를 상속하지 않고 로컬 지오메트리(정점 데이터)만 공유하므로 실제 배치엔 영향 없음 —
  각 에셋의 실측 로컬 바운딩박스로 길이/높이 축을 확인 후 구현 (레일: 로컬 Z가 진행방향, 지지대: 로컬 Y가 높이,
  둘 다 별도 회전 보정 불필요).

## cart.js 밸런스 패널티(speed *= 0.995) dt 독립성 점검 (재확인)
- **구조 확인**: `_evaluateSegment(dt)`는 `Cart.update(dt)` 끝에서 호출되고, `update(dt)`는 오직
  `main.js`의 `Game._fixedUpdate(dt)`에서만 호출되며, 그 호출부는 `while (accumulator >= FIXED_DT) { this._fixedUpdate(FIXED_DT); ... }`
  구조라 dt 인자는 항상 상수 `FIXED_DT = 1/60`. 즉 **현재 코드 흐름상 dt는 절대 1/60 외의 값으로 호출되지 않음** —
  저사양 기기의 프레임 드랍은 이 while문이 한 렌더 프레임에 여러 번 도는 것으로 흡수되고(각 호출은 여전히 정확히 1/60초),
  dt 값 자체가 달라지는 경로는 없음. 따라서 "프레임레이트가 달라지면 감쇠율이 달라진다"는 현재 게임 루프에서는
  실제로 발생하지 않는 증상이었음 — 다만 함수 자체가 dt를 받고도 무시하는 건 계약상 버그였고, FIXED_DT를 바꾸거나
  (예: 저사양 대응으로 1/30 전환) 향후 다른 곳에서 다른 dt로 `update()`/`_evaluateSegment()`를 호출하는 순간
  (테스트 하네스, 가변 타임스텝 리팩터링 등) 조용히 잘못된 초당 감쇠율이 나오는 잠재 버그였음.
- **수정**: `speed *= 0.995` → `speed *= Math.pow(0.995, dt * 60)`. dt=1/60일 때 기존과 완전히 동일(0.995^1=0.995).
- **검증**: `_evaluateSegment(dt)`를 높이변화 0·요구 lean을 절대 못 맞추는 가짜 트랙으로 직접 호출해(에너지/마찰 항 배제,
  패널티 한 줄만 순수 격리), 동일한 1초를 60틱(dt=1/60)·30틱(dt=1/30)·120틱(dt=1/120)으로 나눠 비교:
  - 새 공식: 세 경우 모두 speed ≈ 3.70130478848352xx (편차 ~2e-14, 부동소수점 오차 수준) — dt 무관 확인.
  - (대조군) 구 공식이었다면: 60틱=3.701, 30틱=4.302, 120틱=2.740 (편차 최대 1.56, 즉 초당 감쇠율이 최대 2배 넘게 벌어짐) —
    수정 전이었다면 실제로 확인됐을 버그 크기.
  검증 스크립트는 Playwright로 실제 브라우저에서 `window.Cart` 클래스를 직접 구동해 확인(일회성 스크래치, 저장 안 함).

## 전체 플레이 사이클 실입력(Playwright pointer) 시뮬레이션 — 버그 2건 발견
`run-ride-it` 인프라(로컬 서버+Playwright)로 실제 pointerdown/move/up을 캔버스에 흘려 스테이지1을
드래그 스타트→밸런스 스와이프→게이트 탭→완주까지 끝까지 구동해 점검. 두 가지 버그 발견.

1. **(수정함) 조작 입력이 전혀 안 먹던 버그** — `index.html`의 `#uiRoot .screen { pointer-events: auto; }` 규칙이
   `UI.showStartPrompt()`가 렌더하는 전체화면 `.hud` 오버레이(`class="screen hud"`)에도 적용되어, 실제 플레이 중
   캔버스 전체를 투명한 채로 뒤덮고 모든 포인터 이벤트를 가로채고 있었음 — `input.js`의 `InputController`가
   캔버스에 붙인 pointerdown/move/up 리스너가 **단 한 번도 발화하지 않는** 상태였음(드래그 스타트도, 밸런스
   스와이프도, 게이트 탭도 전부 무반응). `document.elementFromPoint(캔버스중앙)`으로 실측 확인 — 캔버스가 아니라
   `.screen.hud` div가 잡힘. 실제 사용자도 마우스/터치로 전혀 플레이할 수 없는 상태였던 것으로 보임(테스트 하네스만의
   문제가 아님). 수정: `#uiRoot .hud { pointer-events: none; }`로 hud 자체는 클릭을 통과시키고, 그 안의 유일한
   실제 버튼인 `.camera-toggle`만 `pointer-events: auto`로 되살림 (특정도가 같은 `#uiRoot .screen`과의 충돌을
   피하려 `#uiRoot .hud` 선택자 사용, 소스 순서상 뒤에 위치해 덮어씀).
2. **(미수정, 판단 필요) `cart.js` `update()`의 `FRICTION * this.speed` 항이 밸런스 패널티와 같은 종류의
   dt 비독립 버그** — `vSquared = speed² + 2*G*dh - FRICTION*speed` 에서 마찰 항이 dt와 무관하게 매 `update()`
   호출마다 고정으로 빠짐. 60Hz 틱마다 매번 적용되다 보니 실측 초당 감쇠가 약 -1.8 m/s/s에 달함(높이변화 0으로
   고정한 가짜 트랙으로 격리 측정: 20틱=1/3초 만에 11.67→11.07, 매틱 -0.03004 고정). 스테이지1 기준 런칭
   직후(speed≈11.67) 약 5초(309틱) 만에 `MIN_SPEED`(2 m/s) 바닥까지 떨어지고, 이후 거의 전 구간을 최저속도로
   크리핑 — 350m 트랙을 실제 다 도는 데 3분 가까이 걸림. 밸런스 패널티는 조건부(판정 실패 시)인 반면 이 마찰 항은
   **무조건 매 틱** 적용되므로 체감 영향이 훨씬 큼 — 현재 "카트가 사실상 멈춘 것처럼 느껴지는" 주 원인으로 보임.
   수정 방향은 밸런스 패널티와 같은 패턴(`Math.pow`/`* dt * 60` 형태로 dt 정규화)이 자연스럽지만, FRICTION=0.06
   상수 자체가 "매 틱 고정 감쇠"를 전제로 잡혀있었을 가능성이 있어 dt-정규화만 하면 기존과 동일하게 여전히 매우
   강한 감속(현재의 "고장난" 체감)이 재현됨 — 실제 원하는 마찰 체감(예: 초당 몇 % 감속인지)에 맞춘 재튜닝이
   필요해 밸런스 판단이 필요한 사안으로 보고만 하고 코드는 건드리지 않음.
3. 정상 동작 확인된 부분: 드래그 스타트(pull strength→launch speed), 밸런스 스와이프(정타 시 combo 누적,
   반대방향 오조작 시 combo 리셋+감속 페널티 적용 확인), 게이트 없는 구간 탭→'none' 응답, 피니쉬 게이트
   타이밍창 근접 탭→'miss'/'good' 판정 응답, t=1 도달 시 `UI.showResult()`로 실제 결과화면 렌더(`Score: 0` —
   스테이지1엔 `airtimeZone` 구간이 없고 에어타임 홀드도 하지 않아 점수 소스가 없으므로 0이 맞음, 버그 아님).
   전 과정 console/page error 없음.
4. 부가 관찰: 스테이지1의 커브 세그먼트 `leanWindow: 0.5`가 매우 관대해서 `leanInput=0`(입력 없음)도 판정
   범위 안에 들어와 버림(target=±0.3, window=0.5 → 허용범위가 사실상 전체 절반). 튜토리얼 스테이지라 의도된
   설계일 수 있으나, "밸런스 조작이 실제로 필요한 난이도인지"는 별도로 확인이 필요해 보임 — 버그로 보진 않고
   관찰 사항으로만 기록.

## 대형 스프린트: 물리 재튜닝 · 카트 정렬 · flick 스타트 · 5스테이지 폐곡선 재설계 · 뱅킹 · 진행률 UI
8단계 요청을 순서대로 전부 진행, 각 단계 스크린샷/로그로 확인 후 다음 단계로 넘어감. 최종 5스테이지 전체
회귀까지 통과.

1. **`cart.js` 마찰 dt 독립 재튜닝** — `FRICTION*speed`를 에너지보존 항(`v²=v0²+2gΔh`)에서 완전히 분리하고
   `speed *= Math.pow(FRICTION_RETAIN_PER_SECOND, dt)` (상수 0.975)로 재작성. 격리 검증: 평지·무입력 기준
   정확히 초당 2.5% 감쇠, dt=1/60·1/30·1/120로 나눠도 편차 ~8e-14(부동소수점 수준)로 dt 무관 확인. 10m
   급낙하 1틱 검증: 10→17.2m/s로 에너지항이 압도적 — 오르막/내리막에서 마찰 체감이 묻히는 것도 확인.
2. **카트 레일 중앙 정렬 버그** — t=0.3에서 `Track.getPositionAt`과 실제 카트 메시 위치를 비교했을 때 위치 자체는
   거의 일치했지만, t=0.4(스테이지3의 급격한 스윙 지점)에서 카트가 레일과 다른 방향으로 뚜렷하게 틀어져 보임을
   스크린샷으로 확인. 원인: `main.js`가 `cart.glb`의 `meshes[0]`(`__root__`)를 그대로 사용했는데, 이 노드엔
   glTF 좌표계 변환용 미러링(`scaling.z=-1`)+180도 회전이 baked-in 되어 있어 `lookAt()`과 결합 시 급커브에서
   시각적으로 어긋남 — 레일/지지대/스테이션은 이미 `meshes[1]`(실제 지오메트리, 부모분리)을 쓰고 있어 문제 없었음.
   같은 패턴으로 통일(`meshes[1]`을 부모에서 분리해 사용)해 수정, t=0.3·0.4 양쪽 스크린샷으로 재확인.
3. **pull-then-flick 스타트** — `input.js`에 release 직전 100ms(`FLICK_WINDOW_MS`) 구간의 이동 샘플을 저장해
   release 순간 속도(px/ms)를 계산, `REFERENCE_FLICK_VELOCITY` 대비 비율로 0.3~1.6배(`MIN/MAX_FLICK_MULTIPLIER`)
   정규화해 `cart.launch(pullStrength, flickMultiplier)`에 결합(디폴트 1로 기존 호출과 호환). 실제 `page.mouse`
   기반 테스트는 이 환경의 CDP 왕복 지연(호출당 200~350ms)이 100ms 윈도우보다 커서 "느림"만 재현 가능했음 —
   핸들러를 직접 호출해 실제 타이밍을 통제한 결정론적 테스트로 로직 자체를 검증: 느린 릴리스(800ms) 3.5m/s
   vs 순간 릴리스 18.67m/s로 5.3배 차이, 의도한 손맛대로 동작 확인.
4. **stages.js 5스테이지 폐곡선 전면 재설계** — 전 스테이지를 "출발=도착" 스타디움형 폐곡선으로 재설계(마지막
   제어점이 첫 제어점과 매우 가깝고 진입 방향도 유사). 오르막/내리막을 더 촘촘히 배치하고 Y값 편차 확대. 모티브는
   유지(2단계 인버티드 루프, 3단계 싱글레일 타이트 밸런스, 4단계 90도 다이브, 5단계 하이브리드 다중 에어타임)하되
   다이나믹함은 스테이지 순서대로 우상향(세그먼트 수 6→8→8→7→9, requiredLean 최고치 0.35→0.5→0.75→0.6→0.8,
   에어타임존 5단계 기준 원본 5개소 → 8개소 내외로 확장). trackLengthM도 소폭 상향 조정(480/1180/960/1080/1680).
5. **track.js 반영** — `CreateCatmullRomSpline`을 `closed:true`로 변경, 상단뷰 스크린샷으로 이음매 없이 순환하는
   것 확인(seam 거리 0.3~0.9 유닛 수준). `_instanceAlongCurve`가 마지막 점→첫 점 구간까지 순회하도록 확장해
   레일/지지대도 이음매 없이 이어짐. `segment.requiredLean>0` 구간에서 `lookAt()`의 `rollCor` 파라미터로 최대
   28도(`MAX_BANK_RAD`, requiredLean에 비례) 뱅킹 적용, `curveDirection`별 부호는 `camera.js`의 기존 롤 연출과
   동일한 규칙(left=음수/right=양수) 사용 — 급커브 구간 스크린샷으로 레일이 뚜렷하게 기울어짐을 확인.
   station_platform은 원래부터 t=0에서 1회만 배치하고 있어 t=1 중복 배치 이슈 자체가 없었음(확인만).
6. **UI "Turn n/n"** — `UI.updateHUD(cart, track)`로 확장해 `track.segmentRanges`에서 `cart.t`가 속한 세그먼트
   인덱스를 찾아 HUD에 표시. 세그먼트 6개 스테이지에서 t=0.05→0.95 구간별로 Turn 1/6~6/6까지 정확히 전환됨을
   확인.
7. **완주 판정 폐곡선 대응** — `isFinished`는 `t>=1`만 보므로 폐곡선 여부와 무관 — 코드 변경 없이 5스테이지 전부
   `cart.update()`로 t=1까지 전진시켜 실제 렌더 루프가 결과화면을 띄우는지 확인, 전부 정상 통과.
8. **최종 5스테이지 전체 회귀** — 실제 pointer 이벤트로 flick 스타트→(물리 빠른 전진)→실제 밸런스 스와이프→
   실제 게이트 탭→(물리 빠른 전진)→실제 피니쉬 탭→완주까지 5스테이지 전부 실행. 전 스테이지 launched:true,
   combo가 스와이프에 반응(변화 확인), 완주 시 결과화면 렌더 확인, **console/page error 5스테이지 전부 0건**.
   스크린샷으로 뱅킹된 레일, Turn n/n·Combo·속도 HUD, 카트가 레일 위에 정확히 위치한 것을 동시에 확인.
   부가 확인: 5단계 스크린샷에서 Score:10 발생 — 실제 원인은 버그가 아니라 스와이프 테스트의 mouse.down이
   120ms 이상 유지되며 손들기(에어타임 홀드)가 우연히 트리거된 것(실제 기능이 의도대로 동작한다는 방증).
9. **턴 횟수(랩) 옵션** — 스테이지 선택 화면에 `qualityBtn`과 동일한 패턴(버튼 순환 + localStorage)으로
   `lapsBtn` 추가, 1/2/3랩 순환 저장(`rc_laps`, `ui.js`의 `LapsManager`). `Cart` 생성자에 `totalLaps`
   3번째 인자로 전달(`main.js`가 `LapsManager.current` 읽어서 넘김, 디폴트 1로 하위 호환). `cart.js`의
   `update()`에서 `t>=1` 도달 시 `currentLap < totalLaps`면 `currentLap`만 늘리고 `t -= 1`로 오버플로를
   유지한 채 다음 랩으로 감아넘기고(폐곡선이라 이음매 위치 그대로 이어짐), 마지막 랩에서만 `t`를 1로 고정 —
   `isFinished`(`t>=1`)와 `main.js`의 완주 판정 코드는 기존 그대로 두어도 마지막 랩에서만 자연히 참이 됨
   (별도 랩 분기 없이 기존 폐곡선 완주 판정 패턴을 그대로 재사용). HUD `turnLabel`은 `totalLaps>1`일 때만
   `Lap n/총랩 · Turn n/n` 형식으로 표시(1랩 기본값에서는 기존과 동일한 `Turn n/n`만 표시). 검증: `rc_laps=2`로
   localStorage 세팅 후 실제 pointer 이벤트(mouse down/move/up)로 flick 스타트 → `Game._fixedUpdate()`를
   직접 반복 호출해 물리를 결정론적으로 빠르게 전진(기존 항목 3·8과 동일한 테스트 패턴) → 1바퀴째 `t=1` 도달
   지점에서 결과화면 없이 `currentLap`이 2로 넘어가는 것과 2바퀴째 `t=1`에서만 결과화면(`.result`)이 뜨는 것을
   확인, page error 0건.
